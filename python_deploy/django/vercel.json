{
  "builds": [{
    "src": "quote_frontend/wsgi.py",
    "use": "@vercel/python",
    "config": { "maxLambdaSize": "15mb" }
  }],
  "routes": [
    { "src": "/(.*)", "dest": "quote_frontend/wsgi.py" }
  ]
}